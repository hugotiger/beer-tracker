import styled from "styled-components";

const Header = styled.header`
  display: flex;
  align-items: center;
  h1 {
    font-weight: 600;
    font-size: 3.2rem;
  }
  span {
    margin-left: 8px;
    font-size: 4.8rem;
  }
`;

export const Styled = { Header };
